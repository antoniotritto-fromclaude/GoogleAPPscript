version: '3.8'

services:
  crm:
    build: .
    container_name: crm-antonio-tritto
    ports:
      - "3000:3000"
    volumes:
      # Persiste il database
      - crm-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  crm-data:
